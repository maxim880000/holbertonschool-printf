#ifndef MAIN_H
#define MAIN_H

/* library that we need to use variadic functions, write and malloc, free */
#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>

int _printf(const char *format, ...);


#endif